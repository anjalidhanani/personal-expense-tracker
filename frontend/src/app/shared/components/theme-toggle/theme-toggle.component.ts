import { Component } from '@angular/core';

@Component({
  selector: 'app-theme-toggle',
  standalone: true,
  template: `<!-- Theme toggle removed - only light theme supported -->`,
  styles: []
})
export class ThemeToggleComponent {
  // Component disabled - only light theme supported
}
